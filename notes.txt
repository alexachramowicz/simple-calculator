Functionality
-new case: operand-operator-operant-operator
    -should evaluate the first operation then use the
     output of that into the input of the next operator
-add unary operator?
-add decimals? 
    -need to check how we're passing parameters to 
     binaryOperate()

UI
-highlight an operator button when clicked and waiting 
 for secondOperand
-better the UI
    -round edges for containers
    -add gap and padding/margin b/w buttons
